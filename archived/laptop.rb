dep 'laptop' do
  requires 'github config'

  requires 'all projects'
  requires 'quicklook plugins'

  requires 'emacs'
  # requires 'intellij'
  requires 'omnigraffle'
  requires 'sizeup'
  # requires 'vagrant'
end
